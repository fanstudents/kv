name: CI

on:
  workflow_dispatch:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  # PR 的新 commit 不必等舊版 smoke 跑完；push 到 main 保持獨立，避免取消準備合併的證據。
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Fetch quality comparison base
        env:
          CI_SCOPE_BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
        run: |
          if [ -n "$CI_SCOPE_BASE_SHA" ] && [ "$CI_SCOPE_BASE_SHA" != "0000000000000000000000000000000000000000" ]; then
            git fetch --no-tags --depth=1 origin "$CI_SCOPE_BASE_SHA" || echo "Unable to fetch comparison base; quality classifier will fail closed."
          fi

      - name: Classify quality gate scope
        id: quality-scope
        env:
          CI_SCOPE_BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          CI_SCOPE_HEAD_SHA: ${{ github.sha }}
        run: node scripts/ci-scope.mjs

      - name: Report quality gate skip
        if: steps.quality-scope.outputs.quality-required != 'true'
        run: |
          echo "Heavy quality gate skipped: ${{ steps.quality-scope.outputs.quality-reason }}"

      - name: Set up Node.js
        if: steps.quality-scope.outputs.quality-required == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        if: steps.quality-scope.outputs.quality-required == 'true'
        run: npm ci

      - name: Check install configuration shape
        if: steps.quality-scope.outputs.quality-required == 'true'
        run: npm run verify:config

      - name: Lint
        if: steps.quality-scope.outputs.quality-required == 'true'
        run: npm run lint

      - name: Type-check
        if: steps.quality-scope.outputs.quality-required == 'true'
        run: npm run typecheck

      - name: Run unit tests
        if: steps.quality-scope.outputs.quality-required == 'true'
        run: npm test

      - name: Build production bundle
        if: steps.quality-scope.outputs.quality-required == 'true'
        run: npm run build

      - name: Install browser test runtime
        if: steps.quality-scope.outputs.quality-required == 'true'
        run: npx playwright install --with-deps chromium

      - name: Run browser smoke tests
        if: steps.quality-scope.outputs.quality-required == 'true'
        run: npm run test:e2e:run:smoke

      - name: Upload browser smoke diagnostics
        if: failure() && steps.quality-scope.outputs.quality-required == 'true'
        uses: actions/upload-artifact@v7
        with:
          name: playwright-diagnostics-${{ github.run_attempt }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 7

  schema:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Fetch schema comparison base
        env:
          CI_SCOPE_BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
        run: |
          if [ -n "$CI_SCOPE_BASE_SHA" ] && [ "$CI_SCOPE_BASE_SHA" != "0000000000000000000000000000000000000000" ]; then
            git fetch --no-tags --depth=1 origin "$CI_SCOPE_BASE_SHA" || echo "Unable to fetch comparison base; schema classifier will fail closed."
          fi

      - name: Classify schema gate scope
        id: schema-scope
        env:
          CI_SCOPE_BASE_SHA: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          CI_SCOPE_HEAD_SHA: ${{ github.sha }}
        run: node scripts/ci-scope.mjs

      - name: Report schema gate skip
        if: steps.schema-scope.outputs.schema-required != 'true'
        run: |
          echo "Schema replay skipped: ${{ steps.schema-scope.outputs.schema-reason }}"

      - name: Set up Node.js
        if: steps.schema-scope.outputs.schema-required == 'true'
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm

      - name: Install locked dependencies
        if: steps.schema-scope.outputs.schema-required == 'true'
        run: npm ci

      - name: Rehearse local migrations
        if: steps.schema-scope.outputs.schema-required == 'true'
        run: npm run schema:rehearse

      - name: Check generated database types
        if: steps.schema-scope.outputs.schema-required == 'true'
        run: npm run schema:types:check
